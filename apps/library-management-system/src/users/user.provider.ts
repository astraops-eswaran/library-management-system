
// import { Connection } from 'mongoose';
// import { UserSchema } from '../common/user.schema';
// import { MongodbService } from '../mongodb/database.service';

// export const userProviders = [
//   {
//     provide: 'USER_MODEL',
//     useFactory: async (connection: MongodbService) => (await connection.connect()).connection.model('UserSchema', UserSchema),
//     inject: [MongodbService],
//   },
// ];
